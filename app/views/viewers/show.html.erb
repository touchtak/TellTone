<!--ビューワー情報-->
<div class="row p-3 mb-3" style="border-bottom:1px solid #aaa">
  <div class="col-md-1">
    <%= image_tag(@viewer.get_viewer_icon, class: "img-fluid", style: "max-width: 100%; max-height: 100%; object-fit: contain;") %>
  </div>

  <div class="col-md-11">
    <% if @viewer.user_id == current_user.id %>
      <div class="float-right">
        <%= link_to edit_viewer_path do %>
          <div class="btn-group btn-group-md">
            <div class="btn btn-outline-secondary">
              <i class="fa-solid fa-wrench fa-lg"></i>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="float-right">
        <% if current_user.viewer.viewer_following?(@viewer) %>
          <%= link_to "フォロー中", viewer_unfollow_path(@viewer.id), method: :delete %>
        <% else %>
          <%= link_to "フォロー", viewer_follow_path(@viewer.id), method: :post %>
        <% end %>
      </div>
    <% end %>

    <div style="display:flex;">
      <h4 class="mb-0 mr-2"><%= @viewer.name %></h4>
      <p><small class="text-body-secondary">id: </small><%= @viewer.user.name %></p>
    </div>

    <div>
      <p><%= @viewer.introduction %></p>
    </div>

    <div>フォロー数: <%= @followings.count %></div>
    <div>フォロワー数: <%= @viewer.viewer_followers.count %></div>
  </div>
</div>

<!--ビューワー投稿-->
<div class="row">

  <!--サイドバー-->
  <div class="col-md-3"></div>

  <!--タイムライン-->
  <div class="col-md-9">
    <%= render 'shared/timeline', posts: @posts %>
  </div>

</div>